1. the "MES.VFS" file needs to be present in the same directory as the executable.
2. The executable has an option menu to perform various actions.
3. When extracting the file it will always create a new folder, so there would be no over-writing.
4. The files will be encoded with shift-jis.
5. The CSV file will be encoded with UTF-8. (probably)